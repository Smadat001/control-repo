---
# Hiera configuration file for Puppet
# This file defines how Hiera will look up data based on the hierarchy and data sources.
version: 5

hierarchy:
  - name: "Per role"
    path: "role/%{trusted.extensions.pp_role}.yaml"

  - name: "Common"
    path: "common.yaml"

